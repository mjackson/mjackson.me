language: node_js
node_js:
  - node
cache: yarn
script: yarn build
branches:
  only:
    - source
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  repo: mjackson/mjackson.github.io
  target_branch: master
  local_dir: build
  on:
    branch: source
